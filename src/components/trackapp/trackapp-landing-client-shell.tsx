"use client";

/** Conservé pour extensions landing client — le shell onboarding vit dans RouteChrome. */
export function TrackappLandingClientShell({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}
